name: real_estate_secure_backend
description: Production-ready backend API for the Real-Estate-Secure platform.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  collection: ^1.18.0
  crypto: ^3.0.3
  dotenv: ^4.2.0
  logging: ^1.2.0
  postgres: ^3.0.8
  shelf: ^1.4.1
  shelf_cors_headers: ^0.1.5
  shelf_router: ^1.1.4
  uuid: ^4.4.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.2
