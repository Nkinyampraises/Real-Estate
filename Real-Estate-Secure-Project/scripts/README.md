# Scripts

Automation and developer tooling.

Suggested contents:
- local dev helpers
- CI utilities
- deployment scripts

## Database
- `scripts/db-migrate.ps1` apply migrations and seeds (requires `psql`)
- `scripts/db-migrate.sh` apply migrations and seeds (requires `psql`)
